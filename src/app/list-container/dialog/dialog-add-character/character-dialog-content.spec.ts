import { CharacterDialogContent } from './character-dialog-content';

describe('CharacterDialogContent', () => {
  it('should create an instance', () => {
    expect(new CharacterDialogContent()).toBeTruthy();
  });
});
