import { TierDialogContent } from './tier-dialog-content';

describe('TierDialogContent', () => {
  it('should create an instance', () => {
    expect(new TierDialogContent()).toBeTruthy();
  });
});
